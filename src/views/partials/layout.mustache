<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TravelPlanner - Your Travel Assistant</title>
    
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold text-primary fs-4" href="/">
                <i class="bi bi-airplane-fill me-2"></i>TravelPlanner
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav align-items-center gap-3">
                    {{#isHomepage}}
                    {{^user}}
                    <li class="nav-item">
                        <a class="btn btn-outline-primary px-3 py-2" href="/login">
                            <i class="bi bi-box-arrow-in-right me-1"></i> Log In
                        </a>
                    </li>
                    {{/user}}
                    {{/isHomepage}}

                    {{#isLoginPage}}
                    <li class="nav-item">
                        <a class="btn btn-primary px-3 py-2" href="/signup">
                            <i class="bi bi-person-plus me-1"></i> Sign Up
                        </a>
                    </li>
                    {{/isLoginPage}}

                    {{#isSignupPage}}
                    <li class="nav-item">
                        <a class="btn btn-outline-primary px-3 py-2" href="/login">
                            <i class="bi bi-box-arrow-in-right me-1"></i> Log In
                        </a>
                    </li>
                    {{/isSignupPage}}

                    {{#user}}
                    <li class="nav-item">
                        <a class="nav-link fw-medium text-dark" href="/dashboard">
                            <i class="bi bi-house-door me-1"></i> Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link fw-medium text-dark" href="/add-trip">
                            <i class="bi bi-plus-circle me-1"></i> Plan Trip
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link fw-medium text-dark" href="/mytrips">
                            <i class="bi bi-briefcase me-1"></i> My Trips
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link fw-medium text-dark" href="/explore">
                            <i class="bi bi-compass me-1"></i> Explore
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle fw-medium text-dark d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-person-circle me-2"></i>{{user.username}}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow-sm border-0" aria-labelledby="userDropdown">
                            <li><a class="dropdown-item" href="/settings"><i class="bi bi-gear me-2"></i>Settings</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <form action="/logout" method="POST" class="m-0">
                                    <button type="submit" class="dropdown-item text-danger fw-medium">
                                        <i class="bi bi-box-arrow-right me-2"></i>Logout
                                    </button>
                                </form>
                            </li>
                        </ul>
                    </li>
                    {{/user}}

                    {{^isHomepage}}
                    {{^isLoginPage}}
                    {{^isSignupPage}}
                    {{^user}}
                    <li class="nav-item">
                        <a class="btn btn-primary px-3 py-2" href="/signup">
                            <i class="bi bi-person-plus me-1"></i> Sign Up
                        </a>
                    </li>
                    {{/user}}
                    {{/isSignupPage}}
                    {{/isLoginPage}}
                    {{/isHomepage}}
                </ul>
            </div>
        </div>
    </nav>
    
    {{{body}}}
    
    <script type="text/javascript" src="/js/main.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>